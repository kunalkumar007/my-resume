/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.shields.io",
      "res.cloudinary.com",
      "play-lh.googleusercontent.com",
    ],
  },
};

export default nextConfig;
